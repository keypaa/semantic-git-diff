[project]
name = "semantic-git-diff"
version = "0.1.0"
description = "Summarize git diffs in plain English using a local LLM"
requires-python = ">=3.10"
dependencies = [
    "unidiff>=0.7.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "ollama>=0.1.0",
]

[project.scripts]
sgd = "sgd.cli:app"
